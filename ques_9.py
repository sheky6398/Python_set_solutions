# Write a Python program to create a symmetric difference

setc1 = set(["green", "blue"])
setc2 = set(["blue", "yellow"])
print(setc1.symmetric_difference(setc2))
print(setc2.symmetric_difference(setc1))